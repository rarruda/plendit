<%= render layout: "gray_box", locals: {title: "Adresser", id: "locations"} do %>
    <div>
        <ol  class="locations-list">
            <% @user.locations.each do |location| %>
                <li>
                    <% if location.favorite %>
                        <%= evil_icon 'ei-star', size: :s, class: 'locations-list__favorite-button locations-list__favorite-button--gold' %>
                    <% else %>
                        <%= button_to make_favorite_location_path(location), class: "button", form_class: "u-inline" do %>
                            <%= evil_icon 'ei-star', size: :s, class: 'locations-list__favorite-button' %>
                        <% end %>
                    <% end %>
                    <span>
                        <%= "#{location.address_line}, #{location.post_code} #{location.city}" %>
                        <small><%= link_to "Rediger", edit_location_path(location) %></small>
                    </span>
                    <span><%= button_to 'Slett', location, method: :delete, class: "button", form_class: "u-inline u-pull-right", data: {confirm: 'Er du sikker på at du vil slette?'} %></span>
                </li>
            <% end %>
        </ol>
    </div>
<% end %>