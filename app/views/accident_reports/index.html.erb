<p id="notice"><%= notice %></p>

<div class="container">
  <div class="eight columns">
    <% if @bookings.present? %>
      <%= render layout: "gray_box", locals: {title: "Skademeldingskjema", id: "accident-report"} do %>
        <%= render partial: "form" %>
      <% end %>
    <% else %>
      Det finnes ingen eksisterende leieforhold. Ta kontakt med <%= Plendit::Application.config.x.customerservice.email %> hvis du har noen spørsmål eller opplever problemer.

    <% end %>
  </div>
</div>