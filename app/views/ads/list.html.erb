
<%= render partial: "shared/personal_bar", locals: { selected: "Dine annonser" } %>

<div class="container">
    <div class="row">
        <div class="eight columns">
            <%= render layout: "gray_box", locals: {title: "Dine annonser"} do %>
                <p>
                    Under ser du en oversikt over dine annonser. Her kan du
                    se hvilke annonser som er aktive eller inaktive.
                    Om du ønsker å endre selve annonsen, kan du enkelt gjøre
                    dette ved å klikke på annonsen det gjelder. Ønsker du
                    å fjerne eller gjøre en annonse usynlig(inaktiv), kan du
                    raskt og enkelt gjøre dette ved å markere en eller flere
                    annonser og gjøre ditt valg via nedtrekksmenyen under.
                </p>
                <%= button_to "Legg ut en ny annonse", new_ad_path, method: "get", class: "button button-primary" %>
            <% end %>

            <section class="u-push-down">
                <%= render partial: "owned_ad_item", collection: @ads, as: 'ad' %>
            </section>

        </div>
        <div class="four columns">
            <%= render layout: "gray_box", locals: {title: "Sortere"} do %>
                <ul>
                    <li>
                        <a href="#">Vis aktive annonser</a>
                    </li>
                    <li>
                        <a href="#">Vis inaktive annonser</a>
                    </li>
                    <li>
                        <a href="#">Vis alle annonser</a>
                    </li>
                </ul>
            <% end %>

            <%= render layout: "gray_box", locals: {title: "Filter"} do %>
              <!-- fixme: real data here. Should happen in the controller
                probably, and just hand us a @month_filters array of
                title and url -->
                <h5>Siste 6 måneder</h5>
                <ul>
                <%
                  (0..6).each do |i|
                     date_i = Date.today - i.month
                %>
                    <li>
                        <a href="#"><%= date_i.strftime('%B') %></a>
                    </li>
                 <%
                  end
                 %>
                </ul>

                <h5>Sorter per år</h5>
                <ul>
                    <li>
                        <a href="#">2014</a>
                    </li>
                    <li>
                        <a href="#">2015</a>
                    </li>
                </ul>
            <% end %>
        </div>
    </div>
</div>
