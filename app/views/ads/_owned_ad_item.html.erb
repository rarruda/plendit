<%
   # fixme: this is a bit eew. We need to figure out better how to work with ES results
   ad = AdDecorator.new(ad)
%>
<div class="owned-ad-item">
    <h5><%= link_to ad.display_title, edit_ad_path(ad) %> <small><%= ad.display_status %></small></h5>
    <p>
        <%# fixme: set size here %>
        <%= image_tag ad.safe_image_url(:thumb), alt: "", class: "u-pull-right owned-ad-item__thumb" %>
        <%= ad.summary %>
    </p>
    <p>Oppdatert <%= ad.updated_at.to_formatted_s(:plendit_short_datetime) %></p>
</div>