<%= render partial: 'shared/personal_bar', locals: { selected: 'Favoritter' } %>

<div class="container">
    <div class="row">
        <div class="eight columns">
            <%= render layout: 'gray_box', locals: { title: 'Favoritter' } do %>
                Her finner du alle annonsene du har lagt til som favoritt.
             <% end %>

            <div>
                <% if @favorites.empty? %>
                    <p>
                        Du har ikke lagret noen favoritter ennå.
                    </p>
                <% else %>
                    <%= render partial: 'favorite_item', collection: @favorites, as: :favorite %>
                <% end %>
            </div>
        </div>

    </div>
</div>