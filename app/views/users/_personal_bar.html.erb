<div class="personal-bar">
    <div class="container">
        <div class="row">
            <div class="twelve columns">
                <% 
                    if not defined? selected
                        selected = "Kontrollpanel"
                    end

                    links = [
                        { text: "Kontrollpanel", path: "/users" },
                        { text: "Leiehistorikk", path: "#" },
                        { text: "Dine annonser", path: "#" },
                        { text: "Din profil", path: edit_user_registration_path },
                        { text: "Din konto", path: "#" },
                        { text: "Dine Forespørsler", path: "/users/requests" }
                    ]
                    links.map { |e| e[:selected] = e[:text].downcase == selected.downcase }
                %>

                <% links.each do |link| %>
                    <a class="personal-bar__link <%= link[:selected] ? "personal-bar__link--active" : ""%>"
                       href="<%= link[:path] %>"><%= link[:text] %></a>
                <% end %>
             </div>
        </div>
    </div>
</div>