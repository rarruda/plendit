<div class="container">
    <div class="row">
        <div class="twelve columns">

            <h2>Bli en plendit-bruker</h2>
            <p>
                Hvis du allerede har en konto på <strong>Facebook</strong>, hos
                <strong>Google</strong> eller på <strong>SPiD</strong> kan du
                <%= link_to "logge inn", new_user_session_path %> med den uten å opprette en egen
                konto først. Det betyr også at hvis du har en bruke på <strong>VG</strong>,
                <strong>Aftenposten</strong> eller <strong>FINN</strong> allerede, så kan du logge rett inn.
            </p>

            <%= render partial: "users/provider_signups" %>

            <hr>

            <p>
                Hvis du foretrekker det kan du opprette en egen plendit-konto under:
            </p>

            <div>
                <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>

                    <%= devise_error_messages! %>

                    <%= f.hidden_field( :personhood, value: 'natural' ) %>

                    <div class="row">
                        <div class="one-third column">
                            <%= f.label :first_name, "Fornavn:" %>
                            <%= f.text_field :first_name, autofocus: true, class: 'u-full-width'  %>
                        </div>
                    </div>

                    <div class="row">
                        <div class="one-third column">
                            <%= f.label :last_name, "Etternavn:" %>
                            <%= f.text_field :last_name, class: 'u-full-width'  %>
                        </div>
                    </div>

                    <div class="row">
                        <div class="one-third column">
                            <%= f.label :birthday, "Fødselsdato:" %>
                            <%= f.date_field :birthday, class: 'u-full-width'  %>
                        </div>
                    </div>

                    <div class="row">
                        <div class="one-third column">
                            <%= f.label :email, "E-post:" %>
                            <%= f.email_field :email, class: 'u-full-width'  %>
                        </div>
                    </div>

                    <div class="row">
                        <div class="one-third column">
                            <%= f.label :unconfirmed_phone_number, "Telefonnummer:" %>
                            <%= f.text_field :unconfirmed_phone_number, :maxlength=>"8", class: 'u-full-width'  %>
                        </div>
                    </div>

                    <div class="row">
                            <div class="one-third column">
                                <%= f.label :password, 'Passord:' %>
                                <%= f.password_field :password, autocomplete: "off", class: 'u-full-width'  %>
                            </div>
                        </div>

                    <div class="row">
                            <div class="one-third column">
                                <%= f.label :password_confirmation, 'Bekreft passord' %>
                                <%= f.password_field :password_confirmation, autocomplete: "off", class: 'u-full-width'  %>
                        </div>
                    </div>

                    <div class="row">
                        <div class="one-third column">
                            <%= f.submit "Opprett konto", class: "button button-primary" %>
                        </div>
                    </div>
                <% end %>

            </div>

            <hr>

            <h2>Bli bedriftskunde?</h2>

            <p>
                Meld deg inn med <%= link_to 'dette skjemaet.', Plendit::Application.config.x.signup.bedriftskunde_signup_link %>
            </p>

        </div>
    </div>

</div>

