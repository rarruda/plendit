<%= render partial: 'shared/personal_bar', locals: {selected: 'Leiehistorikk'} %>

<div class="container">
    <div class="row">
        <div class="eight columns">
             <%= render layout: 'gray_box', locals: { title: 'Leiehistorikk' } do %>
                Her finner du oversikt over leieforhold hvor du enten
                har leid ut selv, eller leid av andre.
             <% end %>

             <% if @bookings.empty? %>
                 <p>
                     Du har ingen gjennoførte utleieforhold ennå.
                 </p>
            <% else %>
                <div>
                    <%= render partial: 'rental_history_item', collection: @bookings, as: :booking %>
                </div>
            <% end %>
        </div>
    </div>
</div>