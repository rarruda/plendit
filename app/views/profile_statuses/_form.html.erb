<%= form_for(@profile_status) do |f| %>
  <% if @profile_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@profile_status.errors.count, "error") %> prohibited this profile_status from being saved:</h2>

      <ul>
      <% @profile_status.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :status %><br>
    <%= f.text_field :status %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
