<%= render partial: "shared/personal_bar", locals: { selected: "Aktiviteter" } %>

<div class="container">
    <div class="row">
        <div class="nine columns">
            <%= render layout: "gray_box", locals: {title: "Aktiviteter"} do %>
                Her finner du dine siste aktiviteter. Om du ser etter gamle leieforhold
                finner du det under <a href="#">leiehistorikk.</a>
            <% end %>

            <section class="u-push-down">
                <%= render partial: "incoming_request_item", collection: @bookings, as: 'booking' %>
            </section>
        </div>
    </div>
</div>