<div class="container">
    <div class="row">
        <%= render partial: "shared/hero_image", locals: { ad: @ad } %>

        <div class="twelve columns">
            <%= render partial: "shared/ad_title_box", locals: { ad: @ad } %>

            <h2>Leieforespørsel til <%= @ad.user.safe_display_name%></h2>

            <%= render 'form' %>
        </div>
    </div>
</div>

